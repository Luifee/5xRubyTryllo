<div id="board" class="px-4 py-2" data-lists="<%= @lists.to_json(include: :cards) %>">
  <draggable v-model="lists" class="flex" @change="listMoved">
    <List v-for="list in lists" :list="list" :key="list.id"></List>
  </draggable>
</div>

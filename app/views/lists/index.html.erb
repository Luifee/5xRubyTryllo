<h1>Lists</h1>

<div class="mt-2 flex">
  <% @lists.each do |list| %>
    <div class="bg-blue-200 mx-2 w-64 rounded">
      <h2 class="px-3 py-1 font-bold"><%= list.name %></h2>
    </div>
  <% end %>
</div>

<br>

<%= link_to 'New List', new_list_path %>
